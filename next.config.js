/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        loader: 'akamai',
        path: '',
    },
    assetPrefix: '/WilliamsCollection/',
};

module.exports = nextConfig
